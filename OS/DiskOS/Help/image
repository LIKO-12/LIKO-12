The LIKO-12 image object, creatable by 2 ways:
 
1. Using GPU:image(data)
2. By calling imagedata:image() at imagedatas.
 
Functions List:
 
image:draw(<X>,<Y>,[R],[SW],[SH],[Quad])
W,H = image:size()
W = image:width()
H = image:height()
imgData = image:data()
newQuad = image:quad(<X>,<Y>,[W],[H])
 
Type "help image.functionName" for info about each function.